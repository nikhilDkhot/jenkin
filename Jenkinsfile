pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git branch: 'master', url: 'E:\jenkin project'
            }
        }
        // Add other stages here like Build, Test, Deploy
    }
}